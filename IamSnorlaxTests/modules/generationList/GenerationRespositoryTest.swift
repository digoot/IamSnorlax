//
//  GenerationRespositoryTest.swift
//  IamSnorlaxTests
//
//  Created by Diego Otero Mata on 21/11/2020.
//

import Foundation
import Nimble
import Quick
import Cuckoo
import OHHTTPStubs
import RxBlocking
import Alamofire
@testable import IamSnorlax

class GenerationRespositoryTest: QuickSpec {
    
    override func spec() {
        
//        let host = "pokeapi.co"
//
//
//
//        context("fetching generations") {
////            Cuckoo.stub(ApiManager.shared, block: { api in
////                when(api).then { _ in
////
////                }
////            })
//
//            beforeSuite {
//                HTTPStubs.removeAllStubs()
//            }
//
//
//            it("with no connections") {
//                stub(condition: isHost(host)) { _ -> HTTPStubsResponse in
//                    let noConnection = AFError.createURLRequestFailed(error: NSError(domain: NSURLErrorDomain, code: URLError.notConnectedToInternet.rawValue))
//                    return HTTPStubsResponse(error: noConnection)
//                }
////                presenter.viewIsReady()
////                expect(try presenter.generations.toBlocking().first()).to(raiseException())
//            }
//
//            it("error 400") {
//                stub(condition: isHost(host)) { _ -> HTTPStubsResponse in
//                    let data = "".data(using: .utf8)
//                    return HTTPStubsResponse(data: data!, statusCode: 400, headers: nil)
//                }
//            }
//        }
//
    }
    
}
